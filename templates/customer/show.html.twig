{% extends 'base.html.twig' %}

{% block title %}Customer{% endblock %}

{% block body %}
    <h1>Customer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ customer.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ customer.Name }}</td>
            </tr>
            <tr>
                <th>BirthDate</th>
                <td>{{ customer.BirthDate ? customer.BirthDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ customer.Phone }}</td>
            </tr>
             </tbody>
        </table>
        <p><a class="asm" href="{{ path('customer_edit', {'id': customer.id}) }}">Edit</a></p>

        <p><a class="asm" href="{{ path('customer_index') }}">Back to list</a></p>

        <h4>{{ include('customer/delete_form.html.twig') }}</h4>
{% endblock %}
