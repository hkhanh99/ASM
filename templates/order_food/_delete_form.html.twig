<form method="post" action="{{ path('order_food_delete', {'id': order_food.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ order_food.id) }}">
    <button class="btn">Delete</button>
</form>
